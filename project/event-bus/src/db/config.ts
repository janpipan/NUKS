/* import { Sequelize } from 'sequelize';

const dbConnection = new Sequelize('events', 'admin', 'psltest', {
    host: 'postgres://event-bus-postgres-svc:5432',
    dialect: 'postgres',
});

export { dbConnection }; */
